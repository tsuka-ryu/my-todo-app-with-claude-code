pre-commit:
  parallel: true
  commands:
    markdownlint:
      glob: "*.md"
      run: npx markdownlint {staged_files} --fix
      stage_fixed: true
    
    eslint:
      glob: "*.{js,ts,jsx,tsx}"
      run: npm run lint
